
---
产品需求文档（草案）

Toy Dairy · 纪念日 / 倒数日（对标 Days Matter）

▎ 状态：需求草案 · 只读输出
▎ 依据：docs/PRD.md、readme.md、现有 companionDays / MemoryHallPage / Entry.type=memorial
▎ 产品定位：在「玩偶是主角」的前提下，把重要日子做成可管理、可倒数、可回味的手帐模块

---
1. 一句话

让「和玩偶一起过的日子」可数、可等、可纪念：用户为玩偶或共同生活建立纪念日/倒数日，系统用大数字 + 手帐视觉展示「还有 X 天 / 已经 Y 天」，并在节点日触发纪念叙事与分享卡片。

---
2. 为什么做（问题）

┌─────────────────────────────────────────┬────────────────────────────────────────────────┐
│                  现状                   │                      缺口                      │
├─────────────────────────────────────────┼────────────────────────────────────────────────┤
│ 已有 companionDays(toy)（按出生日起算） │ 只能算「认识第几天」，不能管理多个自定义日子   │
├─────────────────────────────────────────┼────────────────────────────────────────────────┤
│ 回忆展厅 /memories/:id                  │ 偏「阶段回顾幻灯片」，不是日常可维护的倒数列表 │
├─────────────────────────────────────────┼────────────────────────────────────────────────┤
│ EntryType 含 memorial                   │ 是单条日志类型，不是「事件日历」实体           │
├─────────────────────────────────────────┼────────────────────────────────────────────────┤
│ 档案页有 milestone 文案                 │ 规则写死、不可配置、无提醒                     │
└─────────────────────────────────────────┴────────────────────────────────────────────────┘

对标 Days Matter 的用户心智：

- 列表里一眼看到「高考还有 87 天 / 在一起第 365 天」
- 每个日子有独立颜色/封面
- 可设重复（每年/每月）
- 到日有仪式感（推送、卡片、文案）

Toy Dairy 差异化： 每个纪念日默认绑定玩偶视角——不只是数字，还有「Luna 说：今天是我们的第 100 天哦」。

---
3. 用户与场景

┌────────────────────┬─────────────────────────────────────────────────────────────┐
│        角色        │                            场景                             │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ 带玩偶旅行的年轻人 │ 记录「第一次一起去海边」「领养日」「生日」                  │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ 手帐/情绪陪伴用户  │ 倒数「下次见面」「期末考」「旅行出发」                      │
├────────────────────┼─────────────────────────────────────────────────────────────┤
│ 演示/评委          │ 3 分钟内展示：建一个倒数日 → 大数字 → 点开纪念信 → 生成卡片 │
└────────────────────┴─────────────────────────────────────────────────────────────┘

核心价值链路：

身份卡（谁）→ 纪念日（何时重要）→ 倒数/正数（还剩几天/已过几天）
→ 节点日叙事（玩偶视角）→ 分享卡片 / 写入成长轴

---
4. 产品原则

1. 数字要大、信息要少 —— 首屏像 Days Matter：事件名 + 大号天数 + 单位。
2. 玩偶是叙事者 —— 每个日子可生成/展示玩偶一句纪念语，不做成冷冰冰工具。
3. 与现有模块共生，不另起 App —— 入口挂在「档案 / 成长 / 我的」；可沉淀为 Entry(type=memorial) 或独立实体。
4. 演示优先 —— Mock 种子数据要有：100 天、生日、下次旅行出发。
5. 手帐感 > 复杂日历 —— 不做完整 GIS/复杂重复规则引擎的第一版。

---
5. 信息架构与入口

5.1 建议入口

┌─────────────────────────┬─────────────────────────────────────────────────────────────────────────┐
│          位置           │                                  行为                                   │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────┤
│ 档案 Tab 当前玩偶卡下方 │ 「纪念日」横向卡片 / 最近一个倒数日                                     │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────┤
│ 档案 → 玩偶档案         │ 「我们的日子」列表入口                                                  │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────┤
│ 成长 Tab                │ 时间轴上标注纪念节点（可选 P1）                                         │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────┤
│ 底栏「＋」              │ 记录方式增加「记一个纪念日」（P1）                                      │
├─────────────────────────┼─────────────────────────────────────────────────────────────────────────┤
│ 独立页                  │ /anniversaries 列表 · /anniversaries/:id 详情 · /anniversaries/new 新建 │
└─────────────────────────┴─────────────────────────────────────────────────────────────────────────┘

5.2 与现有页关系

┌────────────────────────┬────────────────────────────────────────────────────────┐
│          现有          │                          关系                          │
├────────────────────────┼────────────────────────────────────────────────────────┤
│ /memories/:id 回忆展厅 │ 保留为「阶段回顾」；纪念日详情可 CTA「打开回忆展厅」   │
├────────────────────────┼────────────────────────────────────────────────────────┤
│ companionDays          │ 自动生成系统纪念日：「和 {name} 相遇」= birthDate 起算 │
├────────────────────────┼────────────────────────────────────────────────────────┤
│ Entry.memorial         │ 节点日可一键「写成纪念日记」落到成长轴                 │
└────────────────────────┴────────────────────────────────────────────────────────┘

---
6. 核心对象（契约草案）

6.1 Anniversary（纪念日 / 倒数日）

type AnniversaryKind =
  | 'meet'        // 相遇/领养（系统可自动建）
  | 'birthday'    // 玩偶生日
  | 'together'    // 在一起 / 陪伴节点
  | 'trip'        // 旅行出发/归来
  | 'custom'      // 自定义

type CountMode = 'countdown' | 'countup' | 'auto'
// auto：目标日未到 → 倒数；已过 → 正数（Days Matter 常见）

type RepeatRule = 'none' | 'yearly' | 'monthly'

interface Anniversary {
  id: string
  toyId: string              // 绑定玩偶（MVP 必绑）
  title: string              // 「第 100 天」「下次去京都」
  targetDate: string         // YYYY-MM-DD
  kind: AnniversaryKind
  countMode: CountMode
  repeat: RepeatRule
  note?: string              // 主人备注
  aiNote?: string            // 玩偶纪念语
  color?: string             // 卡片主题色 token
  coverUrl?: string          // 可选封面
  pinned?: boolean
  notifyEnabled?: boolean    // MVP 可只存字段，真推送后置
  createdAt: string
}

6.2 派生展示字段（前端计算即可）

┌────────────────┬───────────────────────────────────────────┐
│      字段      │                   含义                    │
├────────────────┼───────────────────────────────────────────┤
│ deltaDays      │ 距今天数（绝对值）                        │
├────────────────┼───────────────────────────────────────────┤
│ direction      │ before 还有 / after 已经 / today 就是今天 │
├────────────────┼───────────────────────────────────────────┤
│ displayLabel   │ 「还有 12 天」「已经 100 天」「就是今天」 │
├────────────────┼───────────────────────────────────────────┤
│ nextOccurrence │ 若 yearly/monthly，下一发生日             │
└────────────────┴───────────────────────────────────────────┘

计算规则（与现有一致）： 本地日历日 0 点对齐；今天 = 目标日 → 0 天「就是今天」。

---
7. 功能范围

7.1 P0（必须能演示）

┌────────────┬──────────────────────────────────────────────────────────────┐
│    故事    │                             验收                             │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 系统默认日 │ 每个玩偶自动有「相遇日」（birthDate）正数展示                │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 列表       │ 按「最近优先」（今天 > 即将到来 > 已过很久）排序；大数字可读 │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 新建       │ 标题 + 日期 + 类型 + 倒数/正数/自动 + 绑定当前玩偶           │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 详情       │ 大数字、目标日、主人备注、玩偶纪念语、编辑/删除              │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 今天高亮   │ 若有「就是今天」的日子，列表顶置 + 仪式文案                  │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 种子数据   │ 至少 3 条：相遇 100 天、生日、未来旅行倒数                   │
├────────────┼──────────────────────────────────────────────────────────────┤
│ 与档案联动 │ 档案页展示「最近一个纪念日」卡片，可点进列表                 │
└────────────┴──────────────────────────────────────────────────────────────┘

7.2 P1（有则加分）

- 生成/重写玩偶纪念语（复用 /api/analyze-entry 风格或新 endpoint）
- 节点日一键写入成长轴 Entry(type=memorial)
- 纪念卡片导出（复用 MemoryHallPage canvas 逻辑，文案改成具体事件）
- 颜色/封面自定义
- yearly 重复（生日每年提醒）
- 置顶、筛选（仅倒数 / 仅正数）

7.3 P2 / Out of scope（本轮不做）

- 系统级推送 / 桌面 Widget
- 多账号云同步（除非真后端就绪）
- 复杂 RRULE、农历、时区专家模式
- 社区晒纪念日 feed
- 付费主题包

---
8. 关键页面与交互

8.1 列表页 /anniversaries

[ 我们的日子 · {玩偶名} ▼ ]
┌─────────────────────────┐
│  就是今天 · 相遇第 100 天 │  ← 顶置仪式卡
│      ★ 100 ★            │
│   已经一起走过 100 天     │
└─────────────────────────┘
┌──────┐ ┌──────┐
│京都出发│ │Luna生日│
│ 还有  │ │ 还有  │
│  23  │ │  41  │
│  天  │ │  天  │
└──────┘ └──────┘
          [ + 添加日子 ]

- 空状态：插画 +「把想一起等待的日子写下来」
- 切换玩偶：与全局 currentToy 同步

8.2 新建/编辑

字段：标题*、日期*、类型、计数方式（自动/倒数/正数）、重复、备注、主题色（可选）
提交后 Toast：「日子已收藏」→ 回列表或详情

8.3 详情

- 大数字 hero
- 元信息：类型标签、重复、目标日
- 「主人想说」/「{玩偶} 想说」双视角（对齐产品原则）
- 操作：编辑、删除、生成纪念语、写成日记、分享卡片

8.4 档案页嵌入

现有 milestone 升级为真实数据源：

- 展示最近 1 个「即将到来」或「今天」
- CTA：「全部日子」→ /anniversaries

---
9. AI 能力（可选但契合产品）

场景： 生成/重写纪念日玩偶语 aiNote

输入：toy 人格、anniversary.title/date/kind、deltaDays、direction、主人 note
输出：短句第一人称（≤80 字），温柔手帐风，不说教

示例：

▎ 「还有 23 天就要去京都了。我会把小包整理好，等你把我放进相机袋里。」

失败则模板兜底（与日记一致）。

---
10. 数据与 Mock

- MVP：localStorage（对齐 mockStore）
- Key 建议：toydairy.anniversaries.v1
- 演示玩偶 toy_luna_demo 种子：
  a. 相遇日 · countup · 100 天（可写死展示与现有 demo 一致）
  b. Luna 生日 · yearly · 未来某日
  c. 「第一次一起看海」周年 · yearly
  d. 「下次旅行出发」· countdown · +N 天

重置演示数据时一并恢复。

---
11. 成功标准

┌────────┬───────────────────────────────────────────────────┐
│  维度  │                       达标                        │
├────────┼───────────────────────────────────────────────────┤
│ 对标感 │ 路人 5 秒内理解：这是倒数日/纪念日列表            │
├────────┼───────────────────────────────────────────────────┤
│ 故事感 │ 能讲清「数字 + 玩偶一句话」                       │
├────────┼───────────────────────────────────────────────────┤
│ 主路径 │ 档案 → 最近日子 → 详情 →（可选）生成卡片 ≤ 1 分钟 │
├────────┼───────────────────────────────────────────────────┤
│ 稳定   │ 刷新不丢；切换玩偶列表正确；删除确认              │
└────────┴───────────────────────────────────────────────────┘

---
12. 开放问题

1. 纪念日是否允许不绑玩偶（全局人生倒数）？建议 MVP 必绑，保持产品焦点。
2. 「第 N 天」是用户手填目标日，还是支持「从某日起第 N 天」反算日期？建议 P1。
3. 与回忆展厅合并入口还是并列？建议：并列——展厅=回顾，纪念日=日程数字。
4. 底栏是否新增 Tab？不建议；用档案入口足够。

---
13. 建议排期（人时粗估）

┌────────────────────────┬───────────────────────────────┬──────┐
│          阶段          │             内容              │  约  │
├────────────────────────┼───────────────────────────────┼──────┤
│ 契约 + Mock + 计算工具 │ types、store、days math、种子 │ 2–3h │
├────────────────────────┼───────────────────────────────┼──────┤
│ 列表 + 新建 + 详情     │ 三页 + 路由                   │ 4–6h │
├────────────────────────┼───────────────────────────────┼──────┤
│ 档案嵌入 + 今天高亮    │ 入口与排序                    │ 1–2h │
├────────────────────────┼───────────────────────────────┼──────┤
│ AI 纪念语 + 卡片复用   │ P1                            │ 2–4h │
├────────────────────────┼───────────────────────────────┼──────┤
│ 打磨空态/动效/演示脚本 │                               │ 1–2h │
└────────────────────────┴───────────────────────────────┴──────┘

---
实现 Prompt（可直接丢给 Claude Code）

Prompt A · 总控（推荐先用）

你在 Toy Dairy（React + TS + Vite + Tailwind，Cloudflare Pages）仓库工作。
只做「对标 Days Matter 的纪念日/倒数日」功能，对齐现有手帐风格与 Mock 架构。

背景（必读）：
- docs/PRD.md、readme.md
- web/src/types.ts（Entry 已有 type memorial，但纪念日应是独立实体 Anniversary）
- web/src/archive/archiveUtils.ts 的 companionDays
- web/src/pages/MemoryHallPage.tsx（回忆展厅，勿替换，可复用卡片导出思路）
- web/src/api/mockStore.ts、web/src/context/AppContext.tsx
- 路由在 web/src/App.tsx；档案首页是 TimelinePage（/archive）

产品目标：
- 用户可为当前玩偶管理多个「日子」：倒数（还有 X 天）/ 正数（已经 Y 天）/ 今天
- 列表大数字优先；详情含主人备注 + 玩偶纪念语
- 每个玩偶自动有一条系统「相遇日」（toy.birthDate）
- 档案页展示最近一个纪念日入口
- 数据 localStorage Mock；重置演示数据要恢复种子

请按顺序：
1) 先补 types + 日期计算工具 + mock store API（CRUD + 按 toyId 查询 + 排序）
2) 再做页面：/anniversaries、/anniversaries/new、/anniversaries/:id
3) 再嵌入档案页入口
4) 种子数据至少 3 条（Luna demo）
5) 不改无关模块；不引入真推送/后端；保持手机 390 宽手帐风

验收：
- 切换玩偶列表跟随
- 新建/编辑/删除可用
- 今天/未来/过去展示文案正确
- typecheck 通过

Prompt B · 数据层

在 Toy Dairy 前端实现 Anniversary Mock 数据层。

新增类型（web/src/types.ts）：
AnniversaryKind = meet | birthday | together | trip | custom
CountMode = countdown | countup | auto
RepeatRule = none | yearly | monthly
Anniversary 字段：id, toyId, title, targetDate, kind, countMode, repeat,
note?, aiNote?, color?, coverUrl?, pinned?, notifyEnabled?, createdAt

工具函数（建议 web/src/anniversary/anniversaryUtils.ts）：
- daysBetween(today, targetDate)
- resolveOccurrence(anniversary, today) // 处理 yearly/monthly 下一发生日
- getAnniversaryView(anniversary, today) => {
    deltaDays, direction: 'before'|'after'|'today',
    displayLabel, unitLabel, nextDate
  }
- sortAnniversaries(list) // today first, then soonest future, then recent past; pinned 优先
- buildDefaultMeetAnniversary(toy) // 系统相遇日

Store（对齐 mockStore 风格）：
listByToy(toyId), get(id), create, update, remove
localStorage key: toydairy.anniversaries.v1
为 toy_luna_demo / toy_bean_demo 写种子；重置演示时重建

把 CRUD 挂到 AppContext（或独立 hook），避免页面直接操作 localStorage。
写少量纯函数单测或至少在工具文件用注释示例覆盖边界：同年、跨年、今天、yearly。

Prompt C · UI 页面

为 Toy Dairy 做纪念日 UI（中文文案，手帐风，复用 PageHeader / card-paper / btn-primary 等现有 class）。

路由：
- /anniversaries 列表
- /anniversaries/new 新建（可用 query toyId，默认 currentToy）
- /anniversaries/:id 详情（含编辑态或 /edit）

列表：
- 顶部切换当前玩偶（可复用档案页 picker 模式）
- 卡片：标题、大号数字、displayLabel、日期小字、kind 标签
- 今天：特殊高亮卡
- FAB 或底部按钮「添加日子」
- 空状态文案温馨

新建表单：
- 标题、日期、类型、计数方式（自动默认）、重复、备注
- 校验：标题非空、日期合法

详情：
- Hero 大数字
- 主人备注 / 玩偶纪念语双块（对齐 Entry 双视角）
- 编辑保存、删除确认
- 次要按钮：「生成纪念语」可用模板先写死；「生成纪念卡片」可先 toast 占位或浅复用 MemoryHall canvas

档案页 TimelinePage：
- 用真实 anniversaries 替换/增强现有 milestone 展示
- 点击进入 /anniversaries 或详情

不要新增底栏 Tab。社区/对话勿动。

Prompt D · AI 纪念语（P1）

为纪念日增加「生成/重写玩偶纪念语」。

优先：本地模板按 kind + direction + traits 拼装，保证离线可用。
可选：扩展 Pages Function 或复用 analyze-entry，新增 purpose=anniversary_note。
Prompt 要求：第一人称、≤80 字、结合玩偶 name/traits/role、事件 title 与还有/已经 X 天、不提 AI。
只更新 anniversary.aiNote；失败 toast + 保留旧文案。

Prompt E · 演示脚本（产品/录屏）

演示（≤90 秒）：
1. 打开档案，看到 Luna「相遇第 100 天」
2. 进入「我们的日子」，指出还有一个未来旅行倒数
3. 新建「第一次一起看烟花」日期选下周，保存
4. 点进详情，读大数字 + 玩偶一句
5. （可选）生成纪念卡片
结束语：玩偶不只出现在照片里，连等待的日子也有了名字。

---
和现有能力的边界（避免做错）

┌───────────────────────────┬──────────────────────────────────────────┐
│           不要            │                    要                    │
├───────────────────────────┼──────────────────────────────────────────┤
│ 用 Entry 硬撑整个倒数列表 │ 独立 Anniversary 实体                    │
├───────────────────────────┼──────────────────────────────────────────┤
│ 拆掉回忆展厅              │ 展厅继续做「回顾」；纪念日做「日子管理」 │
├───────────────────────────┼──────────────────────────────────────────┤
│ 新开底栏 Tab              │ 档案入口 + 独立路由                      │
├───────────────────────────┼──────────────────────────────────────────┤
│ 第一版真推送 Widget       │ 只做 in-app 列表/详情/今天高亮           │
├───────────────────────────┼──────────────────────────────────────────┤
│ 冷冰冰工具风              │ 大数字 + 玩偶语 + 手帐 UI

