# Frontend (public — safe to commit defaults)
# Points to the Pages Function that holds the real provider key.
VITE_AI_ANALYZE_ENDPOINT=/api/analyze-entry

# Optional: real REST API base when USE_MOCK is flipped off in client.ts
# VITE_API_BASE=https://example.com

# --- Cloudflare Pages Function env (NOT Vite, set only in Dashboard) ---
# Project name is toydiary (https://toydiary.pages.dev)
# Settings → Environment variables → Production (and Preview if needed):
#   OPENAI_API_KEY   = <secret, Encrypt>   — key for the gateway (OpenAI or Anthropic)
#   OPENAI_BASE_URL  = https://your-gateway.example/v1
#   OPENAI_MODEL     = claude-3-5-haiku-latest   # or gpt-4o-mini / gateway model id
#   AI_PROVIDER      = auto                    # openai | anthropic | auto
# Claude / Anthropic Messages API is auto-selected when model starts with claude
# or base URL contains anthropic; force with AI_PROVIDER=anthropic.
